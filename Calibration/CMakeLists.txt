cmake_minimum_required(VERSION 2.8)
project( Redev )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS})
add_executable( calibration src/calibrationCamera.cpp )
target_link_libraries( calibration ${OpenCV_LIBS} )
