cmake_minimum_required(VERSION 2.8)
project( Redev )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} include/omnidir.hpp)
add_executable( calibration calibrationCamera.cpp )
target_link_libraries( calibration ${OpenCV_LIBS} )
